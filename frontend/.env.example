# Leave VITE_API_URL empty in dev (Vite proxies /api to the backend).
# In production set it to your backend URL, e.g. https://api.yourdomain.com
VITE_API_URL=

# Your Google OAuth client id (public — safe to expose). Enables the Google button.
VITE_GOOGLE_CLIENT_ID=
